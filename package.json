{
  "name": "fhs-portal",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d"
  },
  "dependencies": {
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}